import React from 'react';
import ReactDOM from 'react-dom';

import { Text } from '@mr/react';

const App = () => {
    return (
        <React.Fragment>
            <Text />
        </React.Fragment>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));